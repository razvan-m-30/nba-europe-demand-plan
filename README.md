# nba-europe-demand-plan

Demand planning for NBA's expansion into the European market.

## Setup

Requires Python 3.13.

```powershell
py -3.13 -m venv .venv
.venv\Scripts\Activate.ps1
pip install -r requirements.txt
```